{
  "scripts": {
    "sync:local": "elog sync -e .elog.env",
    "sync": "elog sync",
    "clean": "elog clean"
  },
  "dependencies": {
    "@elog/cli": "latest",
    "@elog/plugin-adapter": "latest"
  }
}
